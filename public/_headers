/*
  Content-Security-Policy: default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'; script-src 'self' 'unsafe-inline' https://static.cloudflareinsights.com; script-src-attr 'none'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; font-src 'self' data:; connect-src 'self' https://api.emailjs.com https://cloudflareinsights.com; form-action 'self'; frame-src 'none'; manifest-src 'self'; upgrade-insecure-requests
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=(), xr-spatial-tracking=()
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  X-Permitted-Cross-Domain-Policies: none

# Nessuna regola Cache-Control qui: Pages serve i file statici con il suo
# max-age (sw.js esce con max-age=14400) e ignora quello che scriviamo noi.
# Il service worker si aggiorna comunque subito dopo un deploy perché
# src/scripts/components/pwa.js lo registra con updateViaCache:"none", che
# salta la cache HTTP quando il browser controlla se c'è un worker nuovo.

# Lo script che decide la lingua e quelli anti-FOUC sono inline e non possono
# uscire da lì: girano prima del primo paint. Invece di aprire a tutto l'inline
# con 'unsafe-inline', la CSP qui sopra viene riscritta a fine build con gli
# hash SHA-256 di quei quattro script — vedi cspHashesPlugin in vite.config.js.
# Perciò 'unsafe-inline' in script-src vale solo per `npm run dev`: nel dist
# pubblicato non c'è.
